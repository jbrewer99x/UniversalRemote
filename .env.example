# Universal Remote server runtime settings
FIRMWARE_DIR=/data/firmware
FIRMWARE_FILE=universal-remote.bin
FIRMWARE_VERSION_FILE=version.txt

# Optional future secrets. Never put real values in this committed example.
# REMOTE_AGENT_TOKEN=replace-me
# GITHUB_TOKEN=replace-me
